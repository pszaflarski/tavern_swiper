# Tavern Swiper — Profiles Service (Go)
SERVICE_NAME=profiles
SERVICE_PORT=8002
LOG_LEVEL=info

# Infrastructure
FIRESTORE_DATABASE_ID=profiles-dev
GCS_BUCKET_NAME=<project-id>-media-dev

# Pub/Sub
PUBSUB_PROJECT_ID=tavern-swiper-dev
PUBSUB_TOPIC_ID=dev-profiles-profile-events-v1

# JWT — Must match all other services
JWT_SECRET=<shared-jwt-secret-must-match-all-services>
PORT=8002
