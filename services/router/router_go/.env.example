# Tavern Swiper — Router Service (Go)
SERVICE_NAME=router
SERVICE_PORT=8010
LOG_LEVEL=info

# Infrastructure
FIRESTORE_DATABASE_ID=router-dev
GOOGLE_CLOUD_PROJECT=tavern-swiper-dev

# JWT — Must match all other services
JWT_SECRET=<shared-jwt-secret-must-match-all-services>
PORT=8010
