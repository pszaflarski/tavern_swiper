# Tavern Swiper — Characters Service (Go)
SERVICE_NAME=characters
SERVICE_PORT=8012
LOG_LEVEL=info

# Infrastructure
FIRESTORE_DATABASE_ID=characters-dev
GCS_BUCKET_NAME=tavern-swiper-dev-characters-media

# JWT — Must match all other services
JWT_SECRET=super-secret-tavern-key-123
PORT=8012
