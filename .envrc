# Force gcloud to use the Tavern Swiper configuration
export CLOUDSDK_ACTIVE_CONFIG_NAME=tavern

# Optional: If you use a specific project ID frequently in scripts
export GOOGLE_CLOUD_PROJECT=tavern-swiper-dev

# Optional: If you need to point a specific tool to a local key temporarily
# export GOOGLE_APPLICATION_CREDENTIALS="./service-account-key.json"